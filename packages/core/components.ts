import { LButton, LButtonGroup, LIcon, LCol, LRow } from "@lotus-plus/components";
import type { Plugin } from "vue";

export default [
  LButton, 
  LIcon, 
  LButtonGroup, 
  LCol, 
  LRow
] as Plugin[];
